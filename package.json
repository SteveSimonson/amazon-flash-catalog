{
  "name": "amazon-flash-catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-site Amazon flash catalog refresher — admin UI, BSR+API fetch, R2 publish, optional cron",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250726.0",
    "typescript": "^5.8.3",
    "wrangler": "4.26.0"
  }
}
